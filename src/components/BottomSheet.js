import { useEffect } from "react";
export default function BottomSheet({ open, onClose, children, }) { useEffect(() => { if (!open)
    return; }); } // 모달 ?�릴 ??배경 ?�크�??�금    document.documentElement.classList.add("modal-open");    document.body.classList.add("modal-open");        // ESC ?�로 ?�기    const handleEscape = (e: KeyboardEvent) => {      if (e.key === 'Escape' && onClose) {        onClose();      }    };        document.addEventListener('keydown', handleEscape);        return () => {      document.documentElement.classList.remove("modal-open");      document.body.classList.remove("modal-open");      document.removeEventListener('keydown', handleEscape);    };  }, [open, onClose]);  if (!open) return null;    return createPortal(    <>      {/* 배경 ?�릭 ???�기 - ?��? z-index�??�실??배치 */}      <div         className="modal-backdrop"         onClick={onClose}        style={{ zIndex: 999998 }}        role="presentation"        aria-hidden="true"      />            {/* ?�트: ?��? ?�릭??배경?�로 ?�파?��? ?�도�?가??*/}      <div        className="modal-sheet"        role="dialog"         aria-modal="true"        aria-labelledby="modal-title"        onPointerDown={(e) => {          e.stopPropagation();          e.preventDefault();        }}        onClick={(e) => {          e.stopPropagation();          e.preventDefault();        }}        onTouchStart={(e) => e.stopPropagation()}        onTouchMove={(e) => e.stopPropagation()}        style={{           zIndex: 999999,          position: 'fixed',          left: 0,           right: 0,           bottom: 0,          margin: '0 auto',          maxWidth: '640px',          maxHeight: 'min(80dvh, 560px)',          background: '#fff',          borderRadius: '16px 16px 0 0',          padding: '16px',          paddingBottom: 'calc(16px + env(safe-area-inset-bottom, 0px))',          overflow: 'auto',          pointerEvents: 'auto',          boxShadow: '0 -8px 24px rgba(0,0,0,0.12)',          border: '1px solid #e5e7eb'        }}      >        {children}      </div>    </>,    document.body  );} 
