import { Link, useNavigate } from 'react-router-dom';import { getUid } from '../lib/auth';import { safeNavigate } from '../lib/dom';const tabBase =  'px-3 py-2 rounded-lg';export default function BottomTabs() {  const uid = getUid();  const navigate = useNavigate();    return (    <nav      style={{        position: 'fixed', left: 0, right: 0, bottom: 0,        display: 'flex', gap: 8, justifyContent: 'space-around',        padding: '10px', borderTop: '1px solid #eee', background: '#fff',        zIndex: 50      }}    >      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/home')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>??/button>      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/market')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>마켓</button>      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/chat')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>채팅</button>      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/jobs')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>?�자�?/button>      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/groups')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>모임</button>      {uid && (        <button type="button" onClick={(e) => safeNavigate(e, navigate, '/my/products')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>???�품</button>      )}      <button type="button" onClick={(e) => safeNavigate(e, navigate, '/my')} className={`${tabBase} text-gray-700 hover:bg-blue-600 hover:text-white`}>마이</button>      {uid && (        <Link to="/market/create" className={`${tabBase} border`}>          ?�매 ?�록        </Link>      )}    </nav>  );} 