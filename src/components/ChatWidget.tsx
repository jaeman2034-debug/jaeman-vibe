// src/components/ChatWidget.tsximport { useState } from 'react';import { sendChat } from '../apis/chatApi';export default function ChatWidget() {  const [input, setInput] = useState('');  const [logs, setLogs] = useState<string[]>([]);  const [loading, setLoading] = useState(false);  const send = async () => {    const msg = input.trim();    if (!msg) return;    setLoading(true);    setLogs(l => [...l, `?‘¤ ${msg}`]);    setInput('');    try {      const { reply } = await sendChat(msg);      setLogs(l => [...l, `?¤– ${reply}`]);    } catch (e: any) {      setLogs(l => [...l, `??${e.message || 'failed'}`]);    } finally {      setLoading(false);    }  };  return (    <div style={{maxWidth:480, margin:'2rem auto', padding:16, border:'1px solid #eee', borderRadius:12}}>      <h3>Chat Test</h3>      <div style={{height:200, overflow:'auto', padding:8, background:'#fafafa', borderRadius:8, marginBottom:8}}>        {logs.map((line, i) => <div key={i} style={{margin:'4px 0'}}>{line}</div>)}      </div>      <div style={{display:'flex', gap:8}}>        <input          value={input}          onChange={e=>setInput(e.target.value)}          onKeyDown={e=>{ if (e.key==='Enter') send(); }}          placeholder="ë©”ì‹œì§€ ?…ë ¥??          style={{flex:1, padding:'8px 12px', borderRadius:8, border:'1px solid #ddd'}}        />        <button onClick={send} disabled={loading} style={{padding:'8px 12px', borderRadius:8}}>          {loading ? '?„ì†¡ì¤‘â€? : '?„ì†¡'}        </button>      </div>    </div>  );} 