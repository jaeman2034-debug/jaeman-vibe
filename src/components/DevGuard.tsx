import { ReactNode } from "react";import { Navigate, useLocation } from "react-router-dom";import { canAccessDev } from "../lib/devMode";import { getUid } from "../lib/auth";import AppSplash from "./AppSplash";export default function DevGuard({ children }: { children: ReactNode }) {  const uid = getUid();  const loc = useLocation();  // ë¡œë”© ?íƒœë©?? ê¹ ë¹„ì? (uidê°€ null?´ë©´ ë¡œê·¸???ˆë¨)  if (uid === null) return <AppSplash small/>;  if (!canAccessDev({ uid } as any)) {    // ?‘ê·¼ ë¶ˆê? ???ˆìœ¼ë¡?    return <Navigate to="/" replace state={{ from: loc.pathname }} />;  }  return <>{children}</>;} 
