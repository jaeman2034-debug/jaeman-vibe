import { useLocation } from 'react-router-dom';
export default function FloatingMic({ onCommand, onTranscript, isListening = false }) { const { pathname } = useLocation(); } // 마켓 ?�이지?�서??FAB ?�기�?  if (pathname.startsWith('/market')) {    return null;  }  return (    <div className="fixed bottom-6 right-6 z-50">      <button        className="relative w-12 h-12 rounded-full shadow-lg transition-all duration-200 bg-gray-100 hover:bg-gray-200 text-gray-600 flex items-center justify-center"        aria-label="검??      >        <Search className="w-5 h-5" />      </button>    </div>  );} 
