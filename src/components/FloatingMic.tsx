import React from 'react';import { useLocation } from 'react-router-dom';import { Search } from 'lucide-react';type Props = {  onCommand?: (command: any) => void;  onTranscript?: (text: string) => void;  isListening?: boolean;};export default function FloatingMic({ onCommand, onTranscript, isListening = false }: Props) {  const { pathname } = useLocation();    // ë§ˆì¼“ ?˜ì´ì§€?ì„œ??FAB ?¨ê¸°ê¸?  if (pathname.startsWith('/market')) {    return null;  }  return (    <div className="fixed bottom-6 right-6 z-50">      <button        className="relative w-12 h-12 rounded-full shadow-lg transition-all duration-200 bg-gray-100 hover:bg-gray-200 text-gray-600 flex items-center justify-center"        aria-label="ê²€??      >        <Search className="w-5 h-5" />      </button>    </div>  );} 
