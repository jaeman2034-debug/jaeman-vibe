import React from 'react';interface HjdBadgeProps {  status?: 'pending' | 'ok' | 'fail';  hjd?: string; // ?‰ì •???´ë¦„ (?ˆìœ¼ë©??ë™ ?„ë£Œ)  showPending?: boolean; // pending ?íƒœë¥??œì‹œ? ì? ?¬ë?  loc?: { lat: number; lng: number } | null; // ì¢Œí‘œ ?•ë³´ ì¶”ê?}export default function HjdBadge({ status, hjd, showPending = true, loc }: HjdBadgeProps) {  // ì¢Œí‘œê°€ ?†ìœ¼ë©?"ì¢Œí‘œ ?†ìŒ" ?œì‹œ  const hasLoc = !!loc?.lat && !!loc?.lng;  const isOk = status === 'ok' || !!hjd;  const isFail = status === 'fail';    // ì¢Œí‘œê°€ ?†ìœ¼ë©?"ì¢Œí‘œ ?†ìŒ" ?œì‹œ  if (!hasLoc) {    return (      <span className="bg-gray-100 text-gray-600 px-2 py-0.5 rounded-full text-xs font-medium border border-gray-200">        <span className="text-xs">?“</span>        ì¢Œí‘œ ?†ìŒ      </span>    );  }    // hjdê°€ ?ˆìœ¼ë©??ë™?¼ë¡œ ?„ë£Œ ?íƒœë¡?ì²˜ë¦¬  const effectiveStatus = hjd ? 'ok' : (status ?? 'pending');  const s = effectiveStatus;    // pending ?íƒœ?´ê³  showPending??falseë©?ë±ƒì?ë¥??¨ê?  if (s === 'pending' && !showPending) {    return null;  }    const style =    s === 'ok'     ? 'bg-green-100 text-green-700 border-green-200' :    s === 'fail'   ? 'bg-red-100 text-red-700 border-red-200' :                     'bg-amber-100 text-amber-700 border-amber-200';    const text =    s === 'ok'   ? (hjd ? `${hjd} ?•ì¸ ?„ë£Œ` : '?‰ì •???•ì¸ ?„ë£Œ') :    s === 'fail' ? '?‰ì •???•ì¸ ?¤íŒ¨' :                   '?‰ì •???•ì¸ ì¤?;    const icon =    s === 'ok'   ? '?? :    s === 'fail' ? '?? :                   '??;    return (    <span className={`inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs font-medium border ${style}`}>      <span className="text-xs">{icon}</span>      {text}    </span>  );}
