export default function Pagination({  page, pageSize, total, setPage,}: { page:number; pageSize:number; total:number; setPage:(n:number)=>void }) {  const totalPages = Math.max(1, Math.ceil(total / pageSize));  const canPrev = page > 1;  const canNext = page < totalPages;  return (    <div className="mt-4 flex items-center justify-center gap-3">      <button        onClick={() => canPrev && setPage(page-1)}        disabled={!canPrev}        className="btn-ghost disabled:opacity-50"        type="button"      >?´ì „</button>      <div className="rounded-xl border px-3 py-2 text-sm text-slate-600">        {page} / {totalPages}      </div>      <button        onClick={() => canNext && setPage(page+1)}        disabled={!canNext}        className="btn-ghost disabled:opacity-50"        type="button"      >?¤ìŒ</button>    </div>  );}