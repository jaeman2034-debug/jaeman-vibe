import { useState } from "react";import { createProduct } from "@/services/productService";export default function ProductCreateForm() {  const [title, setTitle] = useState("");  const [price, setPrice] = useState<number | "">("");  const [files, setFiles] = useState<File[]>([]);  async function onSubmit(e: React.FormEvent) {    e.preventDefault();    console.log("[PRODUCT] submit start");    try {      const res = await createProduct({        title,        price: Number(price),        files,      });      alert(`?±Î°ù ?ÑÎ£å: ${res.id}`);    } catch (e: any) {      console.error("[PRODUCT] create failed:", e);      alert(`?±Î°ù ?§Ìå®: ${e?.code || e?.message}`);    }  }  return (    <form onSubmit={onSubmit}>      <input value={title} onChange={e=>setTitle(e.target.value)} placeholder="?úÎ™©" required />      <input type="number" value={price} onChange={e=>setPrice(e.target.value===""?"":Number(e.target.value))} placeholder="Í∞ÄÍ≤? required />      <input type="file" multiple onChange={e=>setFiles(Array.from(e.target.files || []))} />      <button type="submit">?±Î°ù</button>    </form>  );} 