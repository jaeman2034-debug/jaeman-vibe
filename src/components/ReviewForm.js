import { useState } from 'react';
export default function ReviewForm({ threadId, targetId, onReviewSubmitted, onCancel }) { const [stars, setStars] = useState(5); const [selectedTags, setSelectedTags] = useState([]); const [loading, setLoading] = useState(false); } // 간단???�그 ?�션  const availableTags = ['친절', '?�간?�속', '깔끔??, '?�직??, '빠른?�답', '불친??, '지�?, '지?�분함', '거짓�?, '?�린?�답'];  const handleTagToggle = (tag: string) => {    setSelectedTags(prev =>       prev.includes(tag)         ? prev.filter(t => t !== tag)        : [...prev, tag]    );  };  const handleSubmit = async (e: React.FormEvent) => {    e.preventDefault();        if (selectedTags.length === 0) {      alert('최소 1�??�상???�그�??�택?�주?�요.');      return;    }    try {      setLoading(true);            await submitReview(threadId, targetId, stars, selectedTags);      alert('?�기가 ?�성?�었?�니??');      onReviewSubmitted?.();    } catch (error: any) {      console.error('?�기 ?�성 ?�패:', error);      alert(error.message || '?�기 ?�성???�패?�습?�다.');    } finally {      setLoading(false);    }  };  return (    <div className="bg-white border rounded-lg p-4 shadow-sm">      <h3 className="text-lg font-semibold mb-4">거래 ?�기 ?�성</h3>            <form onSubmit={handleSubmit} className="space-y-4">        {/* 별점 ?�택 */}        <div>          <label className="block text-sm font-medium text-gray-700 mb-2">            별점          </label>          <div className="flex gap-2">            {[1, 2, 3, 4, 5].map(star => (              <button                key={star}                type="button"                onClick={() => setStars(star)}                className={`text-2xl ${                  star <= stars ? 'text-yellow-400' : 'text-gray-300'                } hover:text-yellow-400 transition-colors`}              >                ??              </button>            ))}          </div>          <p className="text-sm text-gray-500 mt-1">            {stars}?�을 ?�택?�습?�다          </p>        </div>        {/* ?�그 ?�택 */}        <div>          <label className="block text-sm font-medium text-gray-700 mb-2">            ?�그 ?�택 (최소 1�?          </label>          <div className="grid grid-cols-2 gap-2">            {availableTags.map(tag => (              <button                key={tag}                type="button"                onClick={() => handleTagToggle(tag)}                className={`px-3 py-2 text-sm border rounded-lg transition-colors ${                  selectedTags.includes(tag)                    ? 'bg-blue-500 text-white border-blue-500'                    : 'bg-white text-gray-700 border-gray-300 hover:bg-gray-50'                }`}              >                {tag}              </button>            ))}          </div>        </div>        {/* 버튼 */}        <div className="flex gap-2 pt-2">          <button            type="submit"            disabled={loading || selectedTags.length === 0}            className="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:bg-gray-400 disabled:cursor-not-allowed transition-colors"          >            {loading ? '?�출 �?..' : '?�기 ?�출'}          </button>          {onCancel && (            <button              type="button"              onClick={onCancel}              className="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors"            >              취소            </button>          )}        </div>      </form>    </div>  );}
