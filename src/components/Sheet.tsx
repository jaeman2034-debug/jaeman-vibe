import React from 'react';export default function Sheet({  open, onClose, children}: { open: boolean; onClose: () => void; children: React.ReactNode; }) {  if (!open) return null;  return (    <div      onClick={onClose}      style={{        position:'fixed', inset:0, background:'rgba(0,0,0,.35)',        display:'flex', alignItems:'flex-end', zIndex:1000      }}>      <div        onClick={e=>e.stopPropagation()}        style={{          width:'100%', maxWidth:520, margin:'0 auto', background:'#fff',          borderTopLeftRadius:16, borderTopRightRadius:16, padding:16        }}>        <div style={{height:4, width:42, background:'#E5E7EB', borderRadius:999, margin:'0 auto 10px'}}/>        {children}      </div>    </div>  );} 