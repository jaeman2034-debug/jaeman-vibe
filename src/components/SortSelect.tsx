export type SortKey = "newest" | "priceAsc" | "priceDesc" | "distanceAsc";export default function SortSelect({  value, onChange, withDistance=false}: { value: SortKey; onChange:(k:SortKey)=>void; withDistance?:boolean }) {  return (    <select      value={value}      onChange={(e)=>onChange(e.target.value as SortKey)}      className="rounded-xl border border-slate-200 px-4 py-2.5 bg-white"    >      <option value="newest">최신??/option>      <option value="priceAsc">가�??????/option>      <option value="priceDesc">가�??��???/option>      {withDistance && <option value="distanceAsc">거리 가까운??/option>}    </select>  );}
