import { calcTemperature } from '@/services/reviewService';
export default function TemperatureBadge({ stats, showText = true, className = '' }) { const temperature = calcTemperature(stats); } // ?�도???�른 ?�상 결정  const getTemperatureColor = (temp: number) => {    if (temp >= 37.5) return 'text-red-500 bg-red-50 border-red-200';    if (temp >= 37.0) return 'text-orange-500 bg-orange-50 border-orange-200';    if (temp >= 36.5) return 'text-green-500 bg-green-50 border-green-200';    if (temp >= 36.0) return 'text-blue-500 bg-blue-50 border-blue-200';    return 'text-gray-500 bg-gray-50 border-gray-200';  };  const colorClasses = getTemperatureColor(temperature);  return (    <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full border text-sm font-medium ${colorClasses} ${className}`}>      <span className="text-lg">?���?/span>      <span className="font-bold">{temperature.toFixed(1)}°C</span>      {showText && (        <span className="text-xs opacity-80">          {temperature >= 37.5 ? '매우 ?�뜻?? :            temperature >= 37.0 ? '?�뜻?? :            temperature >= 36.5 ? '?�범?? :            temperature >= 36.0 ? '차�??? : '매우 차�???}        </span>      )}      <span className="text-xs opacity-60">({stats.trades}??</span>    </div>  );}
