import React from 'react';import { calcTemperature } from '@/services/reviewService';interface TemperatureBadgeProps {  stats: {    trades: number;    starsSum: number;    pos: number;    neg: number;  };  showText?: boolean;  className?: string;}export default function TemperatureBadge({ stats, showText = true, className = '' }: TemperatureBadgeProps) {  const temperature = calcTemperature(stats);    // ?¨ë„???°ë¥¸ ?‰ìƒ ê²°ì •  const getTemperatureColor = (temp: number) => {    if (temp >= 37.5) return 'text-red-500 bg-red-50 border-red-200';    if (temp >= 37.0) return 'text-orange-500 bg-orange-50 border-orange-200';    if (temp >= 36.5) return 'text-green-500 bg-green-50 border-green-200';    if (temp >= 36.0) return 'text-blue-500 bg-blue-50 border-blue-200';    return 'text-gray-500 bg-gray-50 border-gray-200';  };  const colorClasses = getTemperatureColor(temperature);  return (    <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full border text-sm font-medium ${colorClasses} ${className}`}>      <span className="text-lg">?Œ¡ï¸?/span>      <span className="font-bold">{temperature.toFixed(1)}Â°C</span>      {showText && (        <span className="text-xs opacity-80">          {temperature >= 37.5 ? 'ë§¤ìš° ?°ëœ»?? :            temperature >= 37.0 ? '?°ëœ»?? :            temperature >= 36.5 ? '?‰ë²”?? :            temperature >= 36.0 ? 'ì°¨ê??? : 'ë§¤ìš° ì°¨ê???}        </span>      )}      <span className="text-xs opacity-60">({stats.trades}??</span>    </div>  );}
