import { useState, useRef } from 'react';
import { useAIFeatures } from '../../hooks/useAIFeatures';
export const MultiImageUpload = ({ onAnalysisComplete, onClose, maxFiles = 5, className = '' }) => { const fileInputRef = useRef(null); const [selectedFiles, setSelectedFiles] = useState([]); const [dragActive, setDragActive] = useState(false); const [customPrompt, setCustomPrompt] = useState(''); const { isAnalyzing, lastAnalysis, lastError, analysisProgress, analyzeMultipleImages, clearAnalysis, retryAnalysis, hasAnalysis, hasError, canRetry, progressPercentage } = useAIFeatures(); }; // ?�일 ?�택 처리  const handleFileSelect = useCallback((files: FileList) => {    const newFiles = Array.from(files);    const validFiles: File[] = [];    const errors: string[] = [];    newFiles.forEach(file => {      const validation = validateImageFile(file);      if (validation.valid) {        validFiles.push(file);      } else {        errors.push(`${file.name}: ${getErrorMessage(validation.error || 'invalid-file')}`);      }    });    if (errors.length > 0) {      alert(`?�음 ?�일?�을 ?�로?�할 ???�습?�다:\n${errors.join('\n')}`);    }    if (validFiles.length > 0) {      const totalFiles = selectedFiles.length + validFiles.length;      if (totalFiles > maxFiles) {        const allowed = maxFiles - selectedFiles.length;        alert(`최�? ${maxFiles}개까지�??�로?�할 ???�습?�다. ${allowed}개만 추�??�니??`);        setSelectedFiles(prev => [...prev, ...validFiles.slice(0, allowed)]);      } else {        setSelectedFiles(prev => [...prev, ...validFiles]);      }    }    clearAnalysis();  }, [selectedFiles, maxFiles, clearAnalysis]);  // ?�일 ?�거  const removeFile = useCallback((index: number) => {    setSelectedFiles(prev => prev.filter((_, i) => i !== index));    clearAnalysis();  }, [clearAnalysis]);  // 모든 ?�일 ?�거  const clearAllFiles = useCallback(() => {    setSelectedFiles([]);    clearAnalysis();  }, [clearAnalysis]);  // ?�일 ?�롭 처리  const handleDrop = useCallback((e: React.DragEvent) => {    e.preventDefault();    setDragActive(false);        const files = e.dataTransfer.files;    if (files.length > 0) {      handleFileSelect(files);    }  }, [handleFileSelect]);  // ?�래�??�버 처리  const handleDragOver = useCallback((e: React.DragEvent) => {    e.preventDefault();    setDragActive(true);  }, []);  // ?�래�?리브 처리  const handleDragLeave = useCallback((e: React.DragEvent) => {    e.preventDefault();    setDragActive(false);  }, []);  // ?�중 ?��?지 분석 ?�작  const startAnalysis = useCallback(async () => {    if (selectedFiles.length === 0) return;    try {      const results = await analyzeMultipleImages(selectedFiles, customPrompt);      if (results.length > 0 && onAnalysisComplete) {        onAnalysisComplete(results);      }    } catch (error) {      console.error('?�중 ?��?지 분석 ?�패:', error);    }  }, [selectedFiles, customPrompt, analyzeMultipleImages, onAnalysisComplete]);  // ?�시??  const handleRetry = useCallback(async () => {    if (canRetry && selectedFiles.length > 0) {      await analyzeMultipleImages(selectedFiles, customPrompt);    }  }, [canRetry, selectedFiles, customPrompt, analyzeMultipleImages]);  return (    <div className={`bg-white rounded-lg shadow-lg p-6 max-w-4xl mx-auto ${className}`}>      {/* ?�더 */}      <div className="flex items-center justify-between mb-6">        <h2 className="text-2xl font-bold text-gray-900 flex items-center gap-2">          <ImageIcon className="w-6 h-6 text-blue-600" />          ?�중 ?��?지 AI 분석        </h2>        {onClose && (          <button            onClick={onClose}            className="p-2 hover:bg-gray-100 rounded-full transition-colors"          >            <X className="w-5 h-5 text-gray-500" />          </button>        )}      </div>      {/* ?�일 ?�로???�역 */}      {selectedFiles.length === 0 ? (        <div          className={`border-2 border-dashed rounded-lg p-8 text-center transition-colors cursor-pointer ${            dragActive               ? 'border-blue-400 bg-blue-50'               : 'border-gray-300 hover:border-blue-400'          }`}          onClick={() => fileInputRef.current?.click()}          onDrop={handleDrop}          onDragOver={handleDragOver}          onDragLeave={handleDragLeave}        >          <Upload className={`w-12 h-12 mx-auto mb-4 transition-colors ${            dragActive ? 'text-blue-600' : 'text-gray-400'          }`} />          <p className="text-lg text-gray-600 mb-2">            {dragActive ? '?�기???��?지�??�으?�요' : '?��?지�??�래그하거나 ?�릭?�여 ?�로??}          </p>          <p className="text-sm text-gray-500">            JPG, PNG, WebP (최�? 10MB, 최�? {maxFiles}�?          </p>          <input            ref={fileInputRef}            type="file"            accept="image/*"            multiple            onChange={(e) => {              if (e.target.files) {                handleFileSelect(e.target.files);              }            }}            className="hidden"          />        </div>      ) : (        /* ?�택???�일 목록 �?분석 */        <div className="space-y-4">          {/* ?�일 목록 */}          <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">            {selectedFiles.map((file, index) => (              <div key={index} className="relative bg-gray-50 rounded-lg p-3 border">                <div className="flex items-center gap-2 mb-2">                  <ImageIcon className="w-4 h-4 text-gray-500" />                  <span className="text-sm font-medium text-gray-700 truncate">                    {file.name}                  </span>                  <button                    onClick={() => removeFile(index)}                    className="ml-auto p-1 hover:bg-red-100 rounded-full transition-colors"                  >                    <X className="w-3 h-3 text-red-500" />                  </button>                </div>                                <div className="text-xs text-gray-500">                  {(file.size / 1024 / 1024).toFixed(2)} MB                </div>                                <div className="text-xs text-gray-500">                  {file.type.split('/')[1].toUpperCase()}                </div>              </div>            ))}          </div>          {/* ?�일 관�?버튼 */}          <div className="flex gap-2">            <button              onClick={() => fileInputRef.current?.click()}              className="px-4 py-2 bg-blue-100 text-blue-700 rounded-md hover:bg-blue-200 transition-colors text-sm"            >              ??추�??�기            </button>            <button              onClick={clearAllFiles}              className="px-4 py-2 bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200 transition-colors text-sm"            >              모두 지?�기            </button>          </div>          {/* 커스?� ?�롬?�트 */}          <div>            <label className="block text-sm font-medium text-gray-700 mb-2">              분석 지?�사??(?�택?�항)            </label>            <textarea              value={customPrompt}              onChange={(e) => setCustomPrompt(e.target.value)}              placeholder="?? ???�포츠용?�들??공통 ?�징�?차이?�을 분석?�주?�요"              className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"              rows={2}            />          </div>          {/* 분석 진행�?*/}          {isAnalyzing && (            <div className="space-y-2">              <div className="flex items-center justify-between text-sm text-gray-600">                <span>AI 분석 �?.. ({selectedFiles.length}�??��?지)</span>                <span>{progressPercentage}%</span>              </div>              <div className="w-full bg-gray-200 rounded-full h-2">                <div                   className="bg-blue-600 h-2 rounded-full transition-all duration-300"                  style={{ width: `${progressPercentage}%` }}                ></div>              </div>            </div>          )}          {/* 분석 버튼 */}          <button            onClick={startAnalysis}            disabled={isAnalyzing}            className="w-full bg-blue-600 text-white py-3 px-4 rounded-lg hover:bg-blue-700 disabled:bg-gray-400 disabled:cursor-not-allowed transition-colors flex items-center justify-center gap-2"          >            {isAnalyzing ? (              <>                <RotateCcw className="w-5 h-5 animate-spin" />                AI 분석 �?.. ({progressPercentage}%)              </>            ) : (              <>                <ImageIcon className="w-5 h-5" />                {selectedFiles.length}�??��?지 AI 분석?�기              </>            )}          </button>        </div>      )}      {/* 분석 결과 ?�약 */}      {hasAnalysis && lastAnalysis && (        <div className="mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">          <div className="flex items-center gap-2 mb-4">            <CheckCircle className="w-5 h-5 text-green-600" />            <h3 className="text-lg font-semibold text-green-800">              ?�중 ?��?지 분석 ?�료            </h3>          </div>                    <div className="text-sm text-gray-700 mb-3">            {selectedFiles.length}�??��?지???�??AI 분석???�료?�었?�니??          </div>          {/* �?번째 결과 미리보기 */}          <div className="bg-white rounded p-3 mb-3">            <h4 className="font-medium text-gray-900 mb-2">�?번째 ?��?지 분석 결과</h4>            <div className="space-y-2 text-sm">              <div>                <span className="text-gray-600">카테고리:</span>                <span className="ml-2 px-2 py-1 bg-blue-100 text-blue-800 rounded text-xs">                  {lastAnalysis.category || '기�?'}                </span>              </div>              <div>                <span className="text-gray-600">?�약:</span>                <span className="ml-2 text-gray-700">                  {lastAnalysis.summary || '?�약 ?�음'}                </span>              </div>            </div>          </div>          {/* ?�션 버튼??*/}          <div className="flex gap-2">            <button              onClick={clearAllFiles}              className="px-4 py-2 bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200 transition-colors text-sm"            >              ???��?지�??�시 분석            </button>            {canRetry && (              <button                onClick={handleRetry}                className="px-4 py-2 bg-blue-100 text-blue-700 rounded-md hover:bg-blue-200 transition-colors text-sm flex items-center gap-1"              >                <RotateCcw className="w-4 h-4" />                ?�시??              </button>            )}          </div>        </div>      )}      {/* ?�러 ?�시 */}      {hasError && lastError && (        <div className="mt-6 p-4 bg-red-50 border border-red-200 rounded-lg">          <div className="flex items-center gap-2 mb-2">            <AlertCircle className="w-5 h-5 text-red-600" />            <h3 className="text-lg font-semibold text-red-800">분석 ?�패</h3>          </div>          <p className="text-red-700">{lastError.message}</p>          <div className="flex gap-2 mt-3">            <button              onClick={() => clearAnalysis()}              className="px-4 py-2 bg-red-100 text-red-700 rounded-md hover:bg-red-200 transition-colors text-sm"            >              ?�기            </button>            {canRetry && (              <button                onClick={handleRetry}                className="px-4 py-2 bg-blue-100 text-blue-700 rounded-md hover:bg-blue-200 transition-colors text-sm flex items-center gap-1"              >                <RotateCcw className="w-4 h-4" />                ?�시??              </button>            )}          </div>        </div>      )}    </div>  );}; 
