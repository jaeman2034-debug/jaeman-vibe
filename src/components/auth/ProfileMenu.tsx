import { useState } from "react";import { useAuth } from "../../lib/auth";export default function ProfileMenu() {  const { user, logout } = useAuth();  const [open, setOpen] = useState(false);  if (!user) {    return (      <a href="/login" className="px-3 py-1.5 rounded-xl border hover:bg-zinc-100 dark:hover:bg-zinc-800">ë¡œê·¸??/a>    );  }  // ê¸°ë³¸ ?„ë°”?€ (?¬ìš©???•ë³´ê°€ ?œí•œ?ì´ë¯€ë¡?  const avatar = `https://ui-avatars.com/api/?name=${user.displayName?.charAt(0) || 'U'}&background=random`;  return (    <div className="relative">      <button onClick={() => setOpen((v) => !v)} className="inline-flex items-center gap-2">        <img src={avatar} alt="avatar" className="w-8 h-8 rounded-full border" />        <span className="hidden sm:inline text-sm font-medium max-w-[160px] truncate">          {user.displayName || '?¬ìš©??}        </span>      </button>      {open && (        <div className="absolute right-0 mt-2 w-56 rounded-xl border bg-white dark:bg-zinc-900 shadow-lg p-1">          <a href="/me" className="block px-3 py-2 rounded-lg hover:bg-zinc-100 dark:hover:bg-zinc-800">ë§ˆì´?˜ì´ì§€</a>          <a href="/me/listings" className="block px-3 py-2 rounded-lg hover:bg-zinc-100 dark:hover:bg-zinc-800">???±ë¡?í’ˆ</a>          <button            className="w-full text-left px-3 py-2 rounded-lg hover:bg-zinc-100 dark:hover:bg-zinc-800"            onClick={async () => {               await logout();               location.href = "/";             }}          >ë¡œê·¸?„ì›ƒ</button>        </div>      )}    </div>  );} 