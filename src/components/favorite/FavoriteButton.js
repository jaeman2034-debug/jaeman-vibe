import { useState } from 'react';
import { useAuth } from '@/lib/auth';
export function FavoriteButton({ itemId, size = 'md', showCount = false, className = '', onFavoriteChange }) { const { user } = useAuth(); const [isFavorited, setIsFavorited] = useState(false); const [isLoading, setIsLoading] = useState(false); const [favoriteCount, setFavoriteCount] = useState(0); } // 찜하�??�태 ?�인  useEffect(() => {    if (user) {      checkFavoriteStatus();    }  }, [user, itemId]);  const checkFavoriteStatus = async () => {    if (!user) return;        try {      const favorited = await isFavorite(user.uid, itemId);      setIsFavorited(favorited);    } catch (error) {      console.error('찜하�??�태 ?�인 ?�패:', error);    }  };  // 찜하�??��?  const toggleFavorite = async () => {    if (!user) {      alert('로그?�이 ?�요?�니??');      return;    }    if (isLoading) return;    setIsLoading(true);    try {      if (isFavorited) {        await removeFromFavorites(user.uid, itemId);        setIsFavorited(false);        onFavoriteChange?.(false);      } else {        await addToFavorites(user.uid, itemId);        setIsFavorited(true);        onFavoriteChange?.(true);      }    } catch (error) {      console.error('찜하�??��? ?�패:', error);      alert('찜하�?처리???�패?�습?�다.');    } finally {      setIsLoading(false);    }  };  // ?�기�??��???  const getSizeStyles = () => {    switch (size) {      case 'sm':        return 'w-6 h-6 text-sm';      case 'lg':        return 'w-10 h-10 text-lg';      default: // md        return 'w-8 h-8 text-base';    }  };  // ?�트 ?�이�??�상  const getHeartColor = () => {    if (isFavorited) {      return 'text-red-500 fill-current';    }    return 'text-gray-400 hover:text-red-400';  };  // 로딩 ?�태  if (isLoading) {    return (      <button        disabled        className={`${getSizeStyles()} flex items-center justify-center rounded-full bg-gray-100 text-gray-400 ${className}`}      >        <div className="w-4 h-4 border-2 border-gray-300 border-t-gray-600 rounded-full animate-spin"></div>      </button>    );  }  return (    <div className="flex items-center gap-2">      <button        onClick={(e) => safeAction(e, toggleFavorite)}        className={`${getSizeStyles()} flex items-center justify-center rounded-full hover:bg-red-50 transition-all duration-200 ${getHeartColor()} ${className}`}        title={isFavorited ? '�??�제' : '찜하�?}      >        {isFavorited ? (          // 찜한 ?�태: 채워�??�트          <svg viewBox="0 0 24 24" className="w-5 h-5" fill="currentColor">            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>          </svg>        ) : (          // 찜하지 ?��? ?�태: �??�트          <svg viewBox="0 0 24 24" className="w-5 h-5" fill="none" stroke="currentColor" strokeWidth="2">            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>          </svg>        )}      </button>      {/* 찜한 ?�용?????�시 */}      {showCount && favoriteCount > 0 && (        <span className="text-sm text-gray-500">          {favoriteCount}        </span>      )}    </div>  );}// default??같이 export (import 방식???��??�이 ?�작)export default FavoriteButton; 
