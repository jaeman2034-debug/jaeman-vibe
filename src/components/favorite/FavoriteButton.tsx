import React, { useState, useEffect } from 'react';import { addToFavorites, removeFromFavorites, isFavorite } from '../../services/favoriteService';import { useAuth } from '@/lib/auth';import { safeAction } from '@/lib/dom';interface FavoriteButtonProps {  itemId: string;  size?: 'sm' | 'md' | 'lg';  showCount?: boolean;  className?: string;  onFavoriteChange?: (isFavorited: boolean) => void;}export function FavoriteButton({   itemId,   size = 'md',   showCount = false,  className = '',  onFavoriteChange}: FavoriteButtonProps) {  const { user } = useAuth();  const [isFavorited, setIsFavorited] = useState(false);  const [isLoading, setIsLoading] = useState(false);  const [favoriteCount, setFavoriteCount] = useState(0);  // ì°œí•˜ê¸??íƒœ ?•ì¸  useEffect(() => {    if (user) {      checkFavoriteStatus();    }  }, [user, itemId]);  const checkFavoriteStatus = async () => {    if (!user) return;        try {      const favorited = await isFavorite(user.uid, itemId);      setIsFavorited(favorited);    } catch (error) {      console.error('ì°œí•˜ê¸??íƒœ ?•ì¸ ?¤íŒ¨:', error);    }  };  // ì°œí•˜ê¸?? ê?  const toggleFavorite = async () => {    if (!user) {      alert('ë¡œê·¸?¸ì´ ?„ìš”?©ë‹ˆ??');      return;    }    if (isLoading) return;    setIsLoading(true);    try {      if (isFavorited) {        await removeFromFavorites(user.uid, itemId);        setIsFavorited(false);        onFavoriteChange?.(false);      } else {        await addToFavorites(user.uid, itemId);        setIsFavorited(true);        onFavoriteChange?.(true);      }    } catch (error) {      console.error('ì°œí•˜ê¸?? ê? ?¤íŒ¨:', error);      alert('ì°œí•˜ê¸?ì²˜ë¦¬???¤íŒ¨?ˆìŠµ?ˆë‹¤.');    } finally {      setIsLoading(false);    }  };  // ?¬ê¸°ë³??¤í???  const getSizeStyles = () => {    switch (size) {      case 'sm':        return 'w-6 h-6 text-sm';      case 'lg':        return 'w-10 h-10 text-lg';      default: // md        return 'w-8 h-8 text-base';    }  };  // ?˜íŠ¸ ?„ì´ì½??‰ìƒ  const getHeartColor = () => {    if (isFavorited) {      return 'text-red-500 fill-current';    }    return 'text-gray-400 hover:text-red-400';  };  // ë¡œë”© ?íƒœ  if (isLoading) {    return (      <button        disabled        className={`${getSizeStyles()} flex items-center justify-center rounded-full bg-gray-100 text-gray-400 ${className}`}      >        <div className="w-4 h-4 border-2 border-gray-300 border-t-gray-600 rounded-full animate-spin"></div>      </button>    );  }  return (    <div className="flex items-center gap-2">      <button        onClick={(e) => safeAction(e, toggleFavorite)}        className={`${getSizeStyles()} flex items-center justify-center rounded-full hover:bg-red-50 transition-all duration-200 ${getHeartColor()} ${className}`}        title={isFavorited ? 'ì°??´ì œ' : 'ì°œí•˜ê¸?}      >        {isFavorited ? (          // ì°œí•œ ?íƒœ: ì±„ì›Œì§??˜íŠ¸          <svg viewBox="0 0 24 24" className="w-5 h-5" fill="currentColor">            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>          </svg>        ) : (          // ì°œí•˜ì§€ ?Šì? ?íƒœ: ë¹??˜íŠ¸          <svg viewBox="0 0 24 24" className="w-5 h-5" fill="none" stroke="currentColor" strokeWidth="2">            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>          </svg>        )}      </button>      {/* ì°œí•œ ?¬ìš©?????œì‹œ */}      {showCount && favoriteCount > 0 && (        <span className="text-sm text-gray-500">          {favoriteCount}        </span>      )}    </div>  );}// default??ê°™ì´ export (import ë°©ì‹???ê??†ì´ ?™ì‘)export default FavoriteButton; 
