export default function DistanceBadge({ itemGeo, userLocation, className = '', showIcon = true }) { if (!userLocation) {
    return null;
} } // ?�용???�치가 ?�으�?거리 ?�시 ?�함  }  const distanceText = formatDistanceFrom(itemGeo.lat, itemGeo.lng, userLocation);  // 거리???�른 ?�상 결정  const getDistanceColor = (distanceText: string) => {    if (distanceText.includes('m')) {      const meters = parseInt(distanceText);      if (meters < 1000) return 'bg-green-100 text-green-800 border-green-200'; // 1km 미만      if (meters < 3000) return 'bg-blue-100 text-blue-800 border-blue-200';   // 3km 미만      if (meters < 5000) return 'bg-yellow-100 text-yellow-800 border-yellow-200'; // 5km 미만    }    return 'bg-gray-100 text-gray-800 border-gray-200'; // 5km ?�상  };  return (    <div className={`inline-flex items-center gap-1 px-2 py-1 text-xs border rounded-full font-medium ${getDistanceColor(distanceText)} ${className}`}>      {showIcon && <span>?��</span>}      <span>{distanceText}</span>    </div>  );}// 간단??거리 ?�시 (?�이�??�음)export function SimpleDistance({   itemGeo,   userLocation,   className = '' }: DistanceBadgeProps) {  return (    <DistanceBadge      itemGeo={itemGeo}      userLocation={userLocation}      className={className}      showIcon={false}    />  );} 
