import type { Product } from "./types";import { fmtPrice } from "@/lib/format";import { categoryLabel } from "./categories";export default function ProductCard({ p, onClick }: { p: Product; onClick?: () => void }) {  // 카드?�서 커버 ?�택 (images[0].url ?�선, ?�거??imageUrl 백업)  const cover = (() => {    const firstImage = p?.images?.[0];    if (firstImage && typeof firstImage === 'object' && 'url' in firstImage) {      return firstImage.url;    }    if (firstImage && typeof firstImage === 'string') {      return firstImage;    }    return p?.imageUrl ?? "/placeholder.svg";  })();  return (    <button onClick={onClick} className="text-left group">      <div className="rounded-2xl overflow-hidden border bg-white shadow-sm group-hover:shadow-md transition">        <div className="aspect-[4/3] bg-slate-100">          <img src={cover} alt={p.title} className="w-full h-full object-cover" loading="lazy" />        </div>        <div className="p-3">          <div className="text-xs text-slate-500">{categoryLabel(p.category)}</div>          <div className="font-semibold leading-tight line-clamp-2 mt-0.5">{p.title}</div>          <div className="mt-1 text-blue-600 font-bold">{fmtPrice(p.price)}</div>          {p.location?.region && <div className="mt-0.5 text-xs text-slate-500">{p.location.region} {p.location.city ?? ""}</div>}        </div>      </div>    </button>  );}