"use strict";
// src/features/market/marketService.tsimport { auth, storage, db } from '@/lib/firebase';// ??getStorage import ?�거// import { getStorage } from 'firebase/storage';// ??getStorage() 직접 ?�출 ?�거// const storage = getStorage(app);import { addDoc, collection, serverTimestamp } from 'firebase/firestore';import { getDownloadURL, ref, uploadBytesResumable } from 'firebase/storage';// ?�경변??체크const USE_STORAGE = import.meta.env.VITE_USE_STORAGE === 'true';console.log("[MARKET_SERVICE] USE_STORAGE:", USE_STORAGE);export type NewItemInput = {  title: string;  price: number;  category: string;  condition: '?�상?? | '최상' | '?? | '�? | '??;  description: string;  images: File[];  location?: { lat: number; lng: number } | null;};export async function uploadImages(files: File[], uid: string) {  const urls: string[] = [];    if (!USE_STORAGE) {    console.log('[MARKET_SERVICE] Skipping image upload (USE_STORAGE:', USE_STORAGE, ')');    return urls;  }    console.log('[MARKET_SERVICE] Uploading images to storage...');  for (const file of files) {    const id = `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;    const key = `products/${uid}/${id}-${file.name}`;    const r = ref(storage, key);    const task = uploadBytesResumable(r, file, { contentType: file.type });    await new Promise<void>((resolve, reject) => {      task.on(        'state_changed',        undefined,        reject,        async () => {          const url = await getDownloadURL(task.snapshot.ref);          urls.push(url);          resolve();        }      );    });  }  return urls;}export async function createMarketItem(input: NewItemInput) {  const user = auth.currentUser;  if (!user) throw new Error('로그?�이 ?�요?�니??');  const imageUrls = input.images.length ? await uploadImages(input.images, user.uid) : [];      const docRef = await addDoc(collection(db, 'market_items'), {      title: input.title,      price: input.price,      category: input.category,      condition: input.condition,      description: input.description,      images: imageUrls,      uid: user.uid,      sellerId: user.uid, // ???�수: ?�매??UID 추�? (기존 uid?� 중복?��?�??�환???��?)      seller: {        uid: user.uid,        displayName: user.displayName || null,        email: user.email || null,      },      location: input.location || null,      status: 'active',      createdAt: serverTimestamp(),      updatedAt: serverTimestamp(),    });  return { id: docRef.id, images: imageUrls };} 
