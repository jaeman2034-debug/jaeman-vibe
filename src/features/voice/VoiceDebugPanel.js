"use strict";
// VoiceDebugPanel.tsximport React from "react";import { useSpeech } from "./useSpeech";import UnifiedMicButton from "./UnifiedMicButton"; // ?�서 준 PTT/?��? 겸용 버튼export default function VoiceDebugPanel() {  const { isListening, thresholds, meter, sttSupport } = useSpeech();  const barWidth = Math.min(100, Math.round((meter.rms / (thresholds?.start || 0.02)) * 100));  return (    <div style={{      padding: 12, border: "1px solid #eee", borderRadius: 12, maxWidth: 420, fontFamily: "system-ui",    }}>      <div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 8 }}>        <span style={{          width: 10, height: 10, borderRadius: "50%",          background: isListening ? "#1db954" : "#bbb"        }} />        <b>{isListening ? "Listening" : "Idle"}</b>      </div>      <div style={{ marginBottom: 8, fontSize: 14 }}>        <div>RMS: {meter.rms.toFixed(4)} &nbsp;|&nbsp; dBFS: {Number.isFinite(meter.db) ? meter.db.toFixed(1) : "-??}</div>        <div>start??{thresholds?.start?.toFixed(4) ?? "??} / stop??{thresholds?.stop?.toFixed(4) ?? "??}</div>      </div>      <div style={{ height: 8, background: "#f2f2f2", borderRadius: 6, overflow: "hidden", marginBottom: 12 }}>        <div style={{ width: `${barWidth}%`, height: "100%" }} />      </div>      {!sttSupport.ok ? (        <div style={{           padding: 12,           background: "#fff3cd",           border: "1px solid #ffeaa7",           borderRadius: 8,           color: "#856404",          fontSize: 14        }}>          {sttSupport.reason === "NEED_HTTPS"             ? "HTTPS ?�경???�요?�니?? https://localhost ?�는 배포 URL�??�속??주세??"            : "??브라?��???Web Speech(?�성 ?�식)�??�공?��? ?�습?�다. ?�보?�로 ?�력??주세??"          }        </div>      ) : (        <>          <div style={{ display: "flex", gap: 8 }}>            {/* ?��? 모드 */}            <UnifiedMicButton mode="toggle" />            {/* 길게 ?�러 말하�?PTT) */}            <UnifiedMicButton mode="ptt" />          </div>                <div style={{ marginTop: 8, fontSize: 12, color: "#666" }}>        ???��?: ?�릭?�로 켜고/?�기 &nbsp;??PTT: ?�르???�안�??�기      </div>            {/* ?�경 ??*/}      <div style={{         marginTop: 12,         padding: 12,         background: "#f0f9ff",         border: "1px solid #bae6fd",         borderRadius: 8,         fontSize: 12,        color: "#0369a1"      }}>        <div style={{ fontWeight: 600, marginBottom: 6 }}>?�� ?�경 ??/div>        <ul style={{ margin: 0, paddingLeft: 16, lineHeight: 1.4 }}>          <li>마이???�력 ?�벨??70~90%�??�려보세??/li>          <li>마이?�에 좀 ??가까이 말해보세??/li>          <li>?�크�?창에???�장 ?�로그램 ?�이 ?�스??/li>          <li>localhost ?�는 HTTPS�??�속 (LAN IP???�질 ?�??</li>        </ul>      </div>        </>      )}    </div>  );} 
