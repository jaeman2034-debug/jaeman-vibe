export async function createMarketItem(payload) { const doc = { ...payload, }; } // ???�전?�치: images가 배열?��? ?�인?�고, ?�니�?�?배열�?초기??    images: Array.isArray(payload.images) ? payload.images : [],    createdAt: serverTimestamp(),  };  return addDoc(collection(db, "market"), doc);}/** JOBS */export type JobDoc = {  role: string;  org: string;  location: string;  pay?: string;  desc: string;  lat?: number | null;  lng?: number | null;  images: string[];  createdAt?: any;};export async function createJobPost(payload: any) {  const doc = {    ...payload,    // ???�전?�치: images가 배열?��? ?�인?�고, ?�니�?�?배열�?초기??    images: Array.isArray(payload.images) ? payload.images : [],    createdAt: serverTimestamp(),  };  return addDoc(collection(db, "jobs"), doc);}/** GROUPS */export type GroupDoc = {  name: string;  desc: string;  category: string;  location?: string;  lat?: number | null;  lng?: number | null;  maxMembers?: number;  createdAt?: any;};export async function createGroup(payload: any) {  const doc = {    ...payload,    createdAt: serverTimestamp(),  };  return addDoc(collection(db, "groups"), doc);}
