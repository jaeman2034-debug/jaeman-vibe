const PID = 'jaeman-vibe-platform'; // ?§Ï†ú ?ÑÎ°ú?ùÌä∏ IDconst REGION = 'us-central1';export const FUNCTIONS_BASE =  location.hostname === 'localhost'    ? `http://localhost:5001/${PID}/${REGION}`    : `https://${REGION}-${PID}.cloudfunctions.net`;// ?¨Ïö© ?àÏãú:// import { FUNCTIONS_BASE } from '@/lib/functionsUrl';// await fetch(`${FUNCTIONS_BASE}/analyzeProduct`, { /* ... */ }); 