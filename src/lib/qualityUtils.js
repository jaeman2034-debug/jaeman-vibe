"use strict";
// ?�질 ?��? ?�틸리티export interface QualityLevel {  level: 'low' | 'mid' | 'high';  percentage: number;  message: string;}/** * ?�질 ?�수�??�벨�?메시지�?변?? * @param score 0~1 ?�이???�질 ?�수 * @returns ?�질 ?�벨 ?�보 */export function getQualityLevel(score: number): QualityLevel {  // 0~1 ?�수�?0~100%�?변??  const percentage = Math.round(score * 100);    // ?�계�?기반 ?�벨 ?�정  let level: 'low' | 'mid' | 'high';  if (score < 0.40) {    level = 'low';  } else if (score < 0.70) {    level = 'mid';  } else {    level = 'high';  }    // ?�벨�?메시지  const messages = {    low: '?�질????��?�다. ?�시 촬영??권장?�니??',    mid: '보통?�니?? ?�촬????개선?????�어??',    high: '좋아?? ???�진???�용?�도 ?�니??'  };    return {    level,    percentage,    message: messages[level]  };}/** * ?�질 ?�벨???�른 ?�상 ?�래??반환 */export function getQualityColorClass(level: 'low' | 'mid' | 'high'): string {  const colorClasses = {    low: 'text-red-600 bg-red-50 border-red-200',    mid: 'text-yellow-600 bg-yellow-50 border-yellow-200',    high: 'text-green-600 bg-green-50 border-green-200'  };    return colorClasses[level];}/** * ?�질 ?�벨???�른 ?�이�?반환 */export function getQualityIcon(level: 'low' | 'mid' | 'high'): string {  const icons = {    low: '?�️',    mid: '?��',    high: '??  };    return icons[level];} 
