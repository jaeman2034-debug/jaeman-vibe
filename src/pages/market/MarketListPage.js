import { useSearchParams } from "react-router-dom";
import { usePageAI } from "@/voice/pageAI/usePageAI";
import { makeMarketPlugin } from "./market.ai";
export default function MarketListPage() { usePageAI("market", makeMarketPlugin()); const [sp] = useSearchParams(); const cat = sp.get("cat") || undefined; const sort = sp.get("sort") || undefined; const loc = sp.get("loc") || undefined; } // ?�렬 ?�정  let orderByField = "createdAt";  let orderDirection: "desc" | "asc" = "desc";  if (sort === "price_asc") { orderByField = "price"; orderDirection = "asc"; }  if (sort === "price_desc") { orderByField = "price"; orderDirection = "desc"; }    // ?�터 ?�정  const whereEquals: Array<{ field: string; value: any }> = [];  if (cat) whereEquals.push({ field: "category", value: cat });  if (loc) whereEquals.push({ field: "location", value: loc });    return (    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">      <h1 className="text-2xl font-bold mb-4">?�포�?마켓{cat ? ` ??${cat}` : ''}</h1>      <PageAITips />      <InfiniteGrid         collectionName="products"         whereEquals={whereEquals.length > 0 ? whereEquals : undefined}        orderByField={orderByField}        orderDirection={orderDirection}      />    </div>  );} 
