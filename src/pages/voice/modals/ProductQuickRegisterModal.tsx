import { useState } from "react";import { getAuth } from "firebase/auth";import { createDoc } from "@/lib/writeDoc";export default function ProductQuickRegisterModal({ onClose, preset, imageUrl: imageUrlProp }: { onClose: () => void; preset?: { category?: string; price?: number }; imageUrl?: string }) {  const [title, setTitle] = useState("");  const [category, setCategory] = useState(preset?.category || "");  const [price, setPrice] = useState<number | undefined>(preset?.price);  const [imageUrl, setImageUrl] = useState<string>(imageUrlProp || "");  async function submit() {    const u = getAuth().currentUser; if (!u) return alert("ë¡œê·¸?¸ì´ ?„ìš”?©ë‹ˆ??");    await createDoc("products", { title: title || category || "?í’ˆ", price, imageUrl, category }, u.uid);    onClose();    alert("?±ë¡ ?„ë£Œ");  }  return (    <div className="bg-white dark:bg-zinc-900 rounded-2xl p-4 w-full space-y-3">      <h2 className="text-lg font-semibold">ë¹ ë¥¸ ?í’ˆ ?±ë¡</h2>      <input className="w-full rounded-xl border px-3 py-2" placeholder="?œëª©" value={title} onChange={(e)=>setTitle(e.target.value)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="ì¹´í…Œê³ ë¦¬" value={category} onChange={(e)=>setCategory(e.target.value)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="ê°€ê²? value={price ?? ""} onChange={(e)=>setPrice(parseInt(e.target.value||"0",10)||undefined)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="?´ë?ì§€ URL" value={imageUrl} onChange={(e)=>setImageUrl(e.target.value)} />      <div className="flex gap-2 justify-end">        <button className="px-3 py-1.5 rounded-xl border" onClick={onClose}>ì·¨ì†Œ</button>        <button className="px-3 py-1.5 rounded-xl bg-emerald-600 text-white" onClick={submit}>?±ë¡</button>      </div>    </div>  );} 