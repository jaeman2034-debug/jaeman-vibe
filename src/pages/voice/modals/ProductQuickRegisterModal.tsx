import { useState } from "react";
import { auth } from "@/lib/firebase"; // ???�일 진입???�용
import { createDoc } from "@/lib/writeDoc";export default function ProductQuickRegisterModal({ onClose, preset, imageUrl: imageUrlProp }: { onClose: () => void; preset?: { category?: string; price?: number }; imageUrl?: string }) {  const [title, setTitle] = useState("");  const [category, setCategory] = useState(preset?.category || "");  const [price, setPrice] = useState<number | undefined>(preset?.price);  const [imageUrl, setImageUrl] = useState<string>(imageUrlProp || "");  async function submit() {    const u = getAuth().currentUser; if (!u) return alert("로그?�이 ?�요?�니??");    await createDoc("products", { title: title || category || "?�품", price, imageUrl, category }, u.uid);    onClose();    alert("?�록 ?�료");  }  return (    <div className="bg-white dark:bg-zinc-900 rounded-2xl p-4 w-full space-y-3">      <h2 className="text-lg font-semibold">빠른 ?�품 ?�록</h2>      <input className="w-full rounded-xl border px-3 py-2" placeholder="?�목" value={title} onChange={(e)=>setTitle(e.target.value)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="카테고리" value={category} onChange={(e)=>setCategory(e.target.value)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="가�? value={price ?? ""} onChange={(e)=>setPrice(parseInt(e.target.value||"0",10)||undefined)} />      <input className="w-full rounded-xl border px-3 py-2" placeholder="?��?지 URL" value={imageUrl} onChange={(e)=>setImageUrl(e.target.value)} />      <div className="flex gap-2 justify-end">        <button className="px-3 py-1.5 rounded-xl border" onClick={onClose}>취소</button>        <button className="px-3 py-1.5 rounded-xl bg-emerald-600 text-white" onClick={submit}>?�록</button>      </div>    </div>  );} 
