import { doc, serverTimestamp, setDoc, updateDoc, Timestamp } from 'firebase/firestore';import { auth, db } from '@/lib/firebase';export async function proposeDeal(  threadId: string,  payload: { mode: 'meet'|'delivery'; price: number; place?: string; meetAt?: Date }) {  const u = auth.currentUser; if (!u) throw new Error('ë¡œê·¸?¸ì´ ?„ìš”?©ë‹ˆ??');  const tRef = doc(db, 'threads', threadId);  await setDoc(tRef, {    deal: {      ...payload,      meetAt: payload.meetAt ? Timestamp.fromDate(payload.meetAt) : null,      status: 'proposed',      proposedBy: u.uid,      acceptedBy: null,      updatedAt: serverTimestamp(),    }  }, { merge: true });}export async function acceptDeal(threadId: string, accepterUid: string) {  const tRef = doc(db, 'threads', threadId);  await updateDoc(tRef, {    'deal.status': 'accepted',    'deal.acceptedBy': accepterUid,    'deal.updatedAt': serverTimestamp(),    status: 'booked',    lastMessage: '?½ì†???•ì •?˜ì—ˆ?µë‹ˆ??',    lastMessageAt: serverTimestamp(),  });}export async function cancelDeal(threadId: string) {  const tRef = doc(db, 'threads', threadId);  await updateDoc(tRef, {    'deal.status': 'cancelled',    'deal.updatedAt': serverTimestamp(),    status: 'negotiating',    lastMessage: '?½ì†??ì·¨ì†Œ?˜ì—ˆ?µë‹ˆ??',    lastMessageAt: serverTimestamp(),  });}export async function completeDeal(threadId: string) {  const tRef = doc(db, 'threads', threadId);  await updateDoc(tRef, {    'deal.status': 'completed',    'deal.updatedAt': serverTimestamp(),    status: 'done',    lastMessageAt: serverTimestamp(),  });}