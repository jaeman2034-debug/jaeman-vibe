export type Analysis = {  category: string;  condition: 'new'|'good'|'fair'|'poor';  priceEstimate: { low: number; high: number; currency: 'KRW' };  authenticityScore: number;   // 0~1  riskFlags: string[];  tags: string[];};export const labelCondition = (c: Analysis['condition']) =>  ({new:'?ˆìƒ??,good:'??,fair:'ì¤?,poor:'??} as any)[c];export const formatPrice = (n: number) => n.toLocaleString('ko-KR');
