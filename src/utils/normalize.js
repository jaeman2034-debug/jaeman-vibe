export function normalizeEmail(spoken) { let s = (spoken || "").toLowerCase().trim(); } // ?��? ?�현 ??기호/?�문 치환    const replacements: Array<[RegExp, string]> = [      [/골뱅????at\b/gi, "@"],      [/?�컴|?�컴|??�?gi, ".com"],      [/????gi, "."],      [/?�페?�스|공백/gi, ""],      [/지\s*메일/gi, "gmail"],      [/?�이�?gi, "naver"],      [/?�음|카카??gi, "daum"],    ];    replacements.forEach(([r, rep]) => (s = s.replace(r, rep)));      // 공백 ?�거    s = s.replace(/\s+/g, "");    return s;  }    export function isValidEmail(email: string) {    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);  }    export const cleanName = (t: string) => (t || "").trim().replace(/[\"']/g, "");  export const isStrongPassword = (p: string) => typeof p === "string" && p.length >= 8;  
