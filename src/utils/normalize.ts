export function normalizeEmail(spoken: string) {    let s = (spoken || "").toLowerCase().trim();      // ?œê? ?œí˜„ ??ê¸°í˜¸/?ë¬¸ ì¹˜í™˜    const replacements: Array<[RegExp, string]> = [      [/ê³¨ë±…????at\b/gi, "@"],      [/?·ì»´|?ì»´|??ì½?gi, ".com"],      [/????gi, "."],      [/?¤í˜?´ìŠ¤|ê³µë°±/gi, ""],      [/ì§€\s*ë©”ì¼/gi, "gmail"],      [/?¤ì´ë²?gi, "naver"],      [/?¤ìŒ|ì¹´ì¹´??gi, "daum"],    ];    replacements.forEach(([r, rep]) => (s = s.replace(r, rep)));      // ê³µë°± ?œê±°    s = s.replace(/\s+/g, "");    return s;  }    export function isValidEmail(email: string) {    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);  }    export const cleanName = (t: string) => (t || "").trim().replace(/[\"']/g, "");  export const isStrongPassword = (p: string) => typeof p === "string" && p.length >= 8;  