export function startSpeechRecognition(onResult: (text: string) => void) {    const SR: any = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;    if (!SR) {      alert('??ë¸Œë¼?°ì????Œì„± ?¸ì‹??ì§€?í•˜ì§€ ?ŠìŠµ?ˆë‹¤.');      return;    }    const recognition = new SR();    recognition.lang = 'ko-KR';    recognition.interimResults = false;    recognition.maxAlternatives = 1;      recognition.onresult = (e: any) => {      const text = e.results?.[0]?.[0]?.transcript?.trim?.() ?? '';      if (text) onResult(text);    };    recognition.onerror = () => {      onResult('');    };    recognition.start();  }  