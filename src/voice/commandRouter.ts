import type { Command } from "./intents";import { uiActions } from "./uiActions";export async function routeCommand(cmd: Command) {  const { intent, entities } = cmd;  switch (intent) {    case "NAVIGATE":      return uiActions.navigate(entities);    case "OPEN_MODAL":      return uiActions.openAny(cmd.raw);    case "CAPTURE_PRODUCT":      return uiActions.captureProduct();    case "REGISTER_PRODUCT":      return uiActions.registerProduct(entities);    case "ANALYZE_PRODUCT":      return uiActions.analyzeProduct();    case "LOCATION_ANALYSIS":      return uiActions.locationSearch(entities);    case "HELP":      return uiActions.help();    case "CANCEL":      return uiActions.cancel();  }} 