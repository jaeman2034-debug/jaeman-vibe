import { useEffect } from "react";import { useVoiceAgent } from "../useVoiceAgent";export default function VoiceTestInjector() {  const { handleTranscript } = useVoiceAgent();    useEffect(() => {    const fn = (e: any) => handleTranscript(e.detail);    window.addEventListener("__voice_test__", fn as any);    return () => window.removeEventListener("__voice_test__", fn as any);  }, [handleTranscript]);    return null;} 
