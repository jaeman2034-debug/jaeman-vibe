import { useEffect } from "react";import { setActivePlugin, clearActivePlugin } from "./registry";import type { PageAIContext, PageAIPlugin, PageId } from "./types";import { useNavigate } from "react-router-dom";import { useModal } from "@/components/ModalHost";export function usePageAI(pageId: PageId, makePlugin: (ctx: PageAIContext) => PageAIPlugin) {  const navigate = useNavigate();  const { open } = useModal();  useEffect(() => {    const ctx: PageAIContext = { pageId, navigate, openModal: open };    const plugin = makePlugin(ctx);    setActivePlugin(plugin, ctx);    return () => clearActivePlugin();  }, [pageId, navigate, open, makePlugin]);} 