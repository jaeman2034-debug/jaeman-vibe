import { useCallback, useState } from "react";import { nlu } from "./nlu/simpleNlu";import { routeCommand } from "./commandRouter";import { runPageAI } from "./pageAI/registry";export function useVoiceAgent() {  const [last, setLast] = useState<string>("");  const [lastCmd, setLastCmd] = useState<any>(null);  const handleTranscript = useCallback(async (text: string) => {    setLast(text);    // ???˜ì´ì§€ ?ŒëŸ¬ê·¸ì¸ ?°ì„     const pageHandled = await runPageAI(text);    if (pageHandled) return;    // ???„ì—­ NLU    const cmd = nlu(text);    setLastCmd(cmd);    if (cmd) await routeCommand(cmd);  }, []);  return { handleTranscript, last, lastCmd };} 