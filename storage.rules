rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 이미지 업로드 경로: products/{uid}/{filename}
    match /products/{uid}/{fileId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
} 