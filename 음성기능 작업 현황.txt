좋아! 지금까지 우리가 완성/적용한 기능들을 한눈에 정리해줄게.

구현 완료 목록
UI/UX

✅ 모던 원샷 가입 화면(Tailwind + lucide 아이콘)

✅ 길게 누르고 말하기(PTT) 버튼 + 전역 pointerup/cancel 정리

✅ 최근 인식(누적) 박스: 누적/토글/지우기, 줄바꿈 유지

✅ 전화번호 진행 안내: “앞자리 인식됨: 010-5689-0 → 남은 3자리”

✅ 버튼 활성화 조건: 이름(2–4자) + 전화번호(10–11자리)일 때만 활성

STT(음성 인식) 제어

✅ usePttStt 훅: start()/stop() 공개, continuous, interimResults, maxAlternatives=5

✅ 안정화 스냅샷: 손 뗀 뒤 final → partial → alternatives 폴백

✅ 조각 누적: 최종 결과 조각들을 합쳐 문장 손실 최소화

✅ 마이크 권한 확보(ensureMic) 및 예외 처리

✅ 이벤트 안정화: setPointerCapture 전에 타깃/포인터ID 캐시

음성 → 텍스트 파싱 로직

✅ 이름 추출 강화:

제/내 이름은, 저는/나는, 이름은(단독) 패턴 지원

뒤에 전화/번호/연락처가 와도 인식

꼬리 오인(입/름/늠) 제거, 금지어(전화번호 등) 필터

✅ 숫자 파서(토큰 기반): 숫자/한글숫자 토큰만 변환 → 촬영(…영) 등의 오염 방지

✅ 전화번호 추출: 01x 블록 10–11자리 우선, 1자리 누락 보정

✅ 끝자리 이어붙이기:

pendingDigits(앞자리) 보관

extractTailDigits()로 남은 3–4자리만 말해도 정확히 합치기

성공 시 포맷팅 010-xxxx-xxxx

문자 인증(Firebase)

✅ sendSms() / verifySmsCode() 래퍼

✅ E.164(+82) 변환(toE164KR) 및 입력 숫자만 유지

✅ Invisible reCAPTCHA 싱글톤

✅ 인증 성공 시 updateProfile(displayName) + Firestore users 기록

✅ 에러/로딩 상태 처리

환경/빌드 세팅

✅ .env.local 구성(Firebase 키)

✅ Tailwind v4 + @tailwindcss/postcss 설정, index.css 연결

✅ framer-motion / lucide-react 설치 및 사용

메모(기능 토글)

현재 음성 자동 ON/OFF(VAD 자동 토글) 는 꺼두고, PTT 원샷만 사용 중.

필요 시 VAD 자동 시작/종료는 옵션으로 다시 붙일 수 있음.

다음에 하면 좋은 소소한 개선(선택)

실패 안내 문구 세분화(권한 거부/너무 짧은 발화/재전송 타이머 등)

인증 재전송 60초 타이머, 남은 시도 안내

성공/실패 로깅(성공률 추적)

완료 후 라우팅(다음 단계 화면)

원하는 항목 있으면 바로 붙여줄게. 지금 베이스는 텍스트/음성 모두로 이름·번호 입력 → 문자 인증까지 안정적으로 돌아가요! 🙌

ChatGPT는 실수를 할 수 있습니다. 중요한 정보는